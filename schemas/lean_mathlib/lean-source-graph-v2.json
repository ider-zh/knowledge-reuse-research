{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ider-zh/knowledge-reuse-research/schemas/lean-source-graph-v2.json",
  "title": "Lean resolved source-reference graph v2 atomic usage",
  "description": "The atomic .ilean usage shape is unchanged from v1. In v2 a usage becomes a declaration edge only when parent_decl resolves to an Environment declaration whose defining module equals the usage's .ilean module.",
  "type": "object",
  "required": [
    "snapshot_id",
    "module",
    "parent_decl",
    "target_module",
    "target_decl",
    "start_line",
    "start_character",
    "end_line",
    "end_character"
  ],
  "properties": {
    "snapshot_id": {"type": "string"},
    "module": {"type": "string"},
    "parent_decl": {"type": ["string", "null"]},
    "target_module": {
      "type": "string",
      "description": "Containing-module hint encoded in Lean's resolved constant identity; not part of node identity."
    },
    "target_decl": {"type": "string"},
    "start_line": {"type": "integer", "minimum": 0},
    "start_character": {"type": "integer", "minimum": 0},
    "end_line": {"type": "integer", "minimum": 0},
    "end_character": {"type": "integer", "minimum": 0}
  },
  "additionalProperties": false
}
