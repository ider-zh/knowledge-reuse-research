# Open-source software adapter

The first experiment must choose one graph unit and relation precisely, such as
function → resolved callee, plus a reproducible corpus manifest. Language
adapters emit native tables and graph-core-v1 rather than forcing heterogeneous
languages into an unlabelled graph.
