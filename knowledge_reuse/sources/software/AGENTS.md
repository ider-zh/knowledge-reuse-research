# Open-source software workstream

Owns repository snapshots, language parsing/resolution, graph semantics, and
graph-core mapping. Pin full commits and parser versions. Keep unresolved or
dynamic calls explicit and distinguish call graphs from import/package graphs.
Large data belongs under data/software/<snapshot>/.
